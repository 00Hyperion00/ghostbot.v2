# 4B.4.3.6.6.37B-H1 Install Contract Launcher Alignment Hotfix

Purpose: close the 37B install-contract launcher gap by normalizing BAT launcher install commands to:

```text
python -m pip install -r requirements.txt
```

Scope is limited to BAT launcher normalization and no-submit P0-1 closure evidence. It does not enable paper/live/live-real, network, runtime overlay, training, reload, public observation execution, or order submit.
