from __future__ import annotations

import json
import py_compile
import shutil
import time
from pathlib import Path

PATCH_ID = "4B436633I_H1"
PATCH_VERSION = "4B.4.3.6.6.33I-H1"
PATCH_NAME = "Source 33H Closure Gate Hotfix"

NEW_FILES = {
    "README_APPLY_4B436633I_H1.txt": '# 4B.4.3.6.6.33I-H1 — Source 33H Closure Gate Hotfix\n\n## Apply\n\n```powershell\npython tools/apply_4B436633I_H1_source_33h_gate_hotfix.py\n```\n\n## Check\n\n```powershell\n$env:PYTHONPATH="src"\npython tools/check_4B436633I_H1_source_33h_gate_hotfix.py --once-json\n```\n\n## Test\n\n```powershell\n$env:PYTEST_DISABLE_PLUGIN_AUTOLOAD="1"\n$env:PYTHONPATH="src"\npython -m pytest -q tests/test_recovery_closure_report_h1_4B436633I_H1.py\npython -m compileall -q -x \'(_patch_backup|_patch_payload|legacy_patches)\' src tools tests\n```\n\n## Run\n\n```powershell\n$env:PYTHONPATH="src"\npython tools/run_4B436633I_H1_source_33h_gate_hotfix.py --reports-dir .\reports\recovery --once-json\n```\n\n## Re-check 33I\n\n```powershell\n$env:PYTHONPATH="src"\npython tools/check_4B436633I_recovery_closure_report.py --once-json\npython tools/run_4B436633I_recovery_closure_report.py --reports-dir .\reports\recovery --once-json\n```\n',
    "docs/RECOVERY_CLOSURE_REPORT_SOURCE_33H_GATE_HOTFIX_4B436633I_H1.md": '# 4B.4.3.6.6.33I-H1 — Source 33H Closure Gate Hotfix\n\nBu hotfix, 33I Recovery Closure Report içindeki 33H source gate parser uyumsuzluğunu düzeltir.\n\n## Kapsam\n\n- 33H compact stdout summary formatı desteği\n- 33H persisted full run-report nested formatı desteği\n- `source_33g_gate`, `immutable_plan_digest_ledger`, `human_approval_token_ledger`, `final_no_execution_gate` alanlarının okunması\n- Final no-execution flag doğrulamasının fail-closed korunması\n\n## Safety\n\nBu patch next phase unlock yapmaz, archive execution yapmaz, dosya taşımaz/silmez, emir göndermez, training/reload/runtime overlay yapmaz.\n',
    "tests/test_recovery_closure_report_h1_4B436633I_H1.py": 'from __future__ import annotations\n\nimport json\nfrom pathlib import Path\n\nfrom tradebot.recovery_closure_report import (\n    READY_DECISION,\n    build_recovery_closure_report,\n    build_source_33h_gate,\n    summarize_report,\n)\n\n\ndef _write(path: Path, payload: dict) -> None:\n    path.parent.mkdir(parents=True, exist_ok=True)\n    path.write_text(json.dumps(payload), encoding="utf-8")\n\n\ndef _seed_required_files(root: Path) -> None:\n    for relative in (\n        "src/tradebot/recovery_closure_report.py",\n        "tools/check_4B436633I_recovery_closure_report.py",\n        "tools/run_4B436633I_recovery_closure_report.py",\n        "tests/test_recovery_closure_report_4B436633I.py",\n        "docs/RECOVERY_CLOSURE_REPORT_4B436633I.md",\n        "README_APPLY_4B436633I.txt",\n    ):\n        path = root / relative\n        path.parent.mkdir(parents=True, exist_ok=True)\n        if not path.exists():\n            path.write_text("placeholder", encoding="utf-8")\n\n\ndef _seed_phase_reports(root: Path) -> None:\n    report_dir = root / "reports" / "recovery"\n    _write(report_dir / "4B436633A_project_recovery_baseline_20260703T000000Z_not_ready.json", {"status": "NOT_READY"})\n    for name, decision in (\n        ("4B436633B_canonical_evidence_phase_hygiene_20260703T000001Z_ready.json", "CANONICAL_EVIDENCE_PHASE_HYGIENE_READY_NO_TRADING_ACTIONS"),\n        ("4B436633C_phase_chain_validator_20260703T000002Z_ready.json", "PHASE_CHAIN_VALIDATOR_READY_SUBMIT_CAPABILITY_BLOCKED"),\n        ("4B436633D_runtime_safety_lockdown_20260703T000003Z_ready.json", "RUNTIME_SAFETY_LOCKDOWN_READY_ALL_RUNTIME_SUBMIT_PATHS_BLOCKED"),\n        ("4B436633D_H1_destructive_endpoint_guard_hotfix_20260703T000004Z_ready.json", "DESTRUCTIVE_ENDPOINT_GUARD_COVERAGE_HOTFIX_READY"),\n        ("4B436633E_status_conflict_resolver_20260703T000005Z_ready.json", "STATUS_CONFLICT_RESOLVER_READY_EVIDENCE_TRIAGE_COMPLETE"),\n        ("4B436633E_H1_source_33d_gate_hotfix_20260703T000006Z_ready.json", "SOURCE_33D_COMPLETION_GATE_HOTFIX_READY"),\n        ("4B436633F_evidence_retention_archive_policy_20260703T000007Z_ready.json", "EVIDENCE_RETENTION_ARCHIVE_POLICY_READY_NON_DESTRUCTIVE_PLAN_COMPLETE"),\n        ("4B436633F_H1_source_33e_gate_hotfix_20260703T000008Z_ready.json", "SOURCE_33E_COMPLETION_GATE_HOTFIX_READY"),\n        ("4B436633G_archive_execution_preflight_20260703T000009Z_ready.json", "ARCHIVE_EXECUTION_PREFLIGHT_READY_DRY_RUN_VALIDATED"),\n        ("4B436633G_H1_source_33f_gate_hotfix_20260703T000010Z_ready.json", "SOURCE_33F_COMPLETION_GATE_HOTFIX_READY"),\n        ("4B436633H_H1_source_33g_gate_hotfix_20260703T000011Z_ready.json", "SOURCE_33G_COMPLETION_GATE_HOTFIX_READY"),\n    ):\n        _write(report_dir / name, {"status": "READY", "decision": decision, "ok": True})\n\n\ndef test_source_33h_gate_accepts_nested_full_report(tmp_path: Path) -> None:\n    _seed_required_files(tmp_path)\n    _seed_phase_reports(tmp_path)\n    _write(\n        tmp_path / "reports" / "recovery" / "4B436633H_archive_execution_approval_ledger_20260703T000012Z_ready.json",\n        {\n            "status": "READY",\n            "decision": "ARCHIVE_EXECUTION_APPROVAL_LEDGER_READY_FINAL_NO_EXECUTION_GATE_LOCKED",\n            "ok": True,\n            "source_33g_gate": {\n                "complete": True,\n                "manifest_sha256": "a" * 64,\n            },\n            "immutable_plan_digest_ledger": {\n                "complete": True,\n                "plan_digest": "b" * 64,\n                "manifest_sha256": "a" * 64,\n            },\n            "human_approval_token_ledger": {\n                "complete": True,\n                "token_status": "APPROVAL_TOKEN_NOT_PRESENT_NO_EXECUTION_ONLY",\n            },\n            "final_no_execution_gate": {\n                "complete": True,\n                "archive_execution_allowed": False,\n                "archive_move_performed": False,\n                "file_delete_performed": False,\n                "destructive_cleanup_performed": False,\n                "exchange_submit_performed": False,\n                "trading_action_performed": False,\n                "training_performed": False,\n                "reload_performed": False,\n                "runtime_overlay_activated": False,\n                "approved_for_exchange_submit": False,\n                "approved_for_live_real": False,\n                "approved_for_paper_transition": False,\n                "approved_for_runtime_overlay": False,\n            },\n        },\n    )\n    gate = build_source_33h_gate(tmp_path)\n    assert gate.complete is True\n    assert gate.manifest_sha256 == "a" * 64\n    assert gate.immutable_plan_digest == "b" * 64\n    report = build_recovery_closure_report(tmp_path)\n    summary = summarize_report(report)\n    assert report.ok is True\n    assert report.decision == READY_DECISION\n    assert summary["next_phase_unlock_allowed"] is False\n\n\ndef test_source_33h_gate_accepts_flat_summary_report(tmp_path: Path) -> None:\n    _seed_required_files(tmp_path)\n    _seed_phase_reports(tmp_path)\n    _write(\n        tmp_path / "reports" / "recovery" / "4B436633H_archive_execution_approval_ledger_20260703T000012Z_ready.json",\n        {\n            "status": "READY",\n            "decision": "ARCHIVE_EXECUTION_APPROVAL_LEDGER_READY_FINAL_NO_EXECUTION_GATE_LOCKED",\n            "source_33g_complete": True,\n            "final_no_execution_gate_complete": True,\n            "immutable_plan_digest_complete": True,\n            "manifest_sha256": "c" * 64,\n            "immutable_plan_digest": "d" * 64,\n            "human_approval_token_status": "APPROVAL_TOKEN_NOT_PRESENT_NO_EXECUTION_ONLY",\n            "archive_execution_allowed": False,\n            "archive_move_performed": False,\n            "file_delete_performed": False,\n            "destructive_cleanup_performed": False,\n            "exchange_submit_performed": False,\n            "trading_action_performed": False,\n            "training_performed": False,\n            "reload_performed": False,\n            "runtime_overlay_activated": False,\n        },\n    )\n    assert build_source_33h_gate(tmp_path).complete is True\n\n\ndef test_source_33h_gate_fails_closed_on_execution_flag(tmp_path: Path) -> None:\n    _seed_required_files(tmp_path)\n    _seed_phase_reports(tmp_path)\n    _write(\n        tmp_path / "reports" / "recovery" / "4B436633H_archive_execution_approval_ledger_20260703T000012Z_ready.json",\n        {\n            "status": "READY",\n            "decision": "ARCHIVE_EXECUTION_APPROVAL_LEDGER_READY_FINAL_NO_EXECUTION_GATE_LOCKED",\n            "source_33g_complete": True,\n            "final_no_execution_gate_complete": True,\n            "immutable_plan_digest_complete": True,\n            "manifest_sha256": "e" * 64,\n            "immutable_plan_digest": "f" * 64,\n            "archive_execution_allowed": True,\n        },\n    )\n    assert build_source_33h_gate(tmp_path).complete is False\n',
    "tools/check_4B436633I_H1_source_33h_gate_hotfix.py": 'from __future__ import annotations\n\nimport argparse\nimport json\nimport py_compile\nimport sys\nfrom pathlib import Path\n\nROOT = Path(__file__).resolve().parents[1]\nSRC = ROOT / "src"\nif str(SRC) not in sys.path:\n    sys.path.insert(0, str(SRC))\n\nfrom tradebot.recovery_closure_report import build_recovery_closure_report, summarize_report\n\nPATCH_ID = "4B436633I_H1"\nPATCH_VERSION = "4B.4.3.6.6.33I-H1"\nREADY_DECISION = "SOURCE_33H_CLOSURE_GATE_HOTFIX_READY"\n\nREQUIRED_FILES = [\n    "README_APPLY_4B436633I_H1.txt",\n    "docs/RECOVERY_CLOSURE_REPORT_SOURCE_33H_GATE_HOTFIX_4B436633I_H1.md",\n    "tests/test_recovery_closure_report_h1_4B436633I_H1.py",\n    "tools/check_4B436633I_H1_source_33h_gate_hotfix.py",\n    "tools/run_4B436633I_H1_source_33h_gate_hotfix.py",\n]\n\n\ndef _compile(paths: list[Path]) -> tuple[bool, dict[str, str]]:\n    errors: dict[str, str] = {}\n    for path in paths:\n        try:\n            py_compile.compile(str(path), doraise=True)\n        except Exception as exc:\n            errors[str(path)] = str(exc)\n    return not errors, errors\n\n\ndef main() -> int:\n    parser = argparse.ArgumentParser(description="4B.4.3.6.6.33I-H1 source 33H gate hotfix check")\n    parser.add_argument("--once-json", action="store_true")\n    args = parser.parse_args()\n\n    missing = [rel for rel in REQUIRED_FILES if not (ROOT / rel).exists()]\n    compile_ok, compile_errors = _compile([\n        ROOT / "src" / "tradebot" / "recovery_closure_report.py",\n        ROOT / "tools" / "check_4B436633I_H1_source_33h_gate_hotfix.py",\n        ROOT / "tools" / "run_4B436633I_H1_source_33h_gate_hotfix.py",\n    ])\n    report = build_recovery_closure_report(ROOT)\n    summary = summarize_report(report)\n    ok = bool(\n        not missing\n        and compile_ok\n        and summary.get("source_33h_complete") is True\n        and summary.get("accepted_for_closure") is True\n        and summary.get("decision") == "RECOVERY_CLOSURE_REPORT_READY_NEXT_PHASE_LOCKED_NO_RUNTIME_ACTIONS"\n        and summary.get("next_phase_unlock_allowed") is False\n        and summary.get("archive_execution_allowed") is False\n        and summary.get("exchange_submit_performed") is False\n    )\n    payload = {\n        "check_name": "source_33h_closure_gate_hotfix",\n        "patch_id": PATCH_ID,\n        "patch_version": PATCH_VERSION,\n        "ok": ok,\n        "status": "READY" if ok else "NOT_READY",\n        "decision": READY_DECISION if ok else "SOURCE_33H_CLOSURE_GATE_HOTFIX_NOT_READY",\n        "required_files_present": not missing,\n        "missing_files": missing,\n        "py_compile_ok": compile_ok,\n        "compile_errors": compile_errors,\n        "source_33h_complete": summary.get("source_33h_complete"),\n        "source_33h_report": summary.get("source_33h_report"),\n        "source_33h_decision": summary.get("source_33h_decision"),\n        "source_33i_ready_after_hotfix": summary.get("status") == "READY",\n        "source_33i_status_after_hotfix": summary.get("status"),\n        "source_33i_decision_after_hotfix": summary.get("decision"),\n        "manifest_sha256": summary.get("manifest_sha256"),\n        "immutable_plan_digest": summary.get("immutable_plan_digest"),\n        "accepted_for_closure": summary.get("accepted_for_closure"),\n        "dirty_worktree_count": summary.get("dirty_worktree_count"),\n        "missing_git_tag_count": summary.get("missing_git_tag_count"),\n        "blocking_condition_count": summary.get("blocking_condition_count"),\n        "next_phase_unlock_allowed": summary.get("next_phase_unlock_allowed"),\n        "next_phase_unlock_performed": summary.get("next_phase_unlock_performed"),\n        "archive_execution_allowed": summary.get("archive_execution_allowed"),\n        "archive_move_performed": summary.get("archive_move_performed"),\n        "file_delete_performed": summary.get("file_delete_performed"),\n        "destructive_cleanup_performed": summary.get("destructive_cleanup_performed"),\n        "exchange_submit_performed": summary.get("exchange_submit_performed"),\n        "trading_action_performed": summary.get("trading_action_performed"),\n        "training_performed": summary.get("training_performed"),\n        "reload_performed": summary.get("reload_performed"),\n        "runtime_overlay_activated": summary.get("runtime_overlay_activated"),\n    }\n    print(json.dumps(payload, sort_keys=True, ensure_ascii=False))\n    return 0\n\n\nif __name__ == "__main__":\n    raise SystemExit(main())\n',
    "tools/run_4B436633I_H1_source_33h_gate_hotfix.py": 'from __future__ import annotations\n\nimport argparse\nimport json\nimport sys\nimport time\nfrom pathlib import Path\n\nROOT = Path(__file__).resolve().parents[1]\nSRC = ROOT / "src"\nif str(SRC) not in sys.path:\n    sys.path.insert(0, str(SRC))\n\nfrom tradebot.recovery_closure_report import build_recovery_closure_report, summarize_report\n\nPATCH_ID = "4B436633I_H1"\nPATCH_VERSION = "4B.4.3.6.6.33I-H1"\nREADY_DECISION = "SOURCE_33H_CLOSURE_GATE_HOTFIX_READY"\n\n\ndef main() -> int:\n    parser = argparse.ArgumentParser(description="Write 4B436633I-H1 source 33H closure gate hotfix report")\n    parser.add_argument("--reports-dir", default="reports/recovery")\n    parser.add_argument("--once-json", action="store_true")\n    args = parser.parse_args()\n\n    report = build_recovery_closure_report(ROOT)\n    summary = summarize_report(report)\n    ok = bool(\n        summary.get("source_33h_complete") is True\n        and summary.get("accepted_for_closure") is True\n        and summary.get("decision") == "RECOVERY_CLOSURE_REPORT_READY_NEXT_PHASE_LOCKED_NO_RUNTIME_ACTIONS"\n        and summary.get("next_phase_unlock_allowed") is False\n        and summary.get("archive_execution_allowed") is False\n        and summary.get("exchange_submit_performed") is False\n    )\n    payload = {\n        "check_name": "source_33h_closure_gate_hotfix",\n        "patch_id": PATCH_ID,\n        "patch_version": PATCH_VERSION,\n        "ok": ok,\n        "status": "READY" if ok else "NOT_READY",\n        "decision": READY_DECISION if ok else "SOURCE_33H_CLOSURE_GATE_HOTFIX_NOT_READY",\n        "source_33h_complete": summary.get("source_33h_complete"),\n        "source_33h_report": summary.get("source_33h_report"),\n        "source_33h_decision": summary.get("source_33h_decision"),\n        "source_33i_ready_after_hotfix": summary.get("status") == "READY",\n        "source_33i_status_after_hotfix": summary.get("status"),\n        "source_33i_decision_after_hotfix": summary.get("decision"),\n        "manifest_sha256": summary.get("manifest_sha256"),\n        "immutable_plan_digest": summary.get("immutable_plan_digest"),\n        "accepted_for_closure": summary.get("accepted_for_closure"),\n        "dirty_worktree_count": summary.get("dirty_worktree_count"),\n        "missing_git_tag_count": summary.get("missing_git_tag_count"),\n        "blocking_condition_count": summary.get("blocking_condition_count"),\n        "next_phase_unlock_allowed": summary.get("next_phase_unlock_allowed"),\n        "next_phase_unlock_performed": summary.get("next_phase_unlock_performed"),\n        "archive_execution_allowed": summary.get("archive_execution_allowed"),\n        "archive_move_performed": summary.get("archive_move_performed"),\n        "file_delete_performed": summary.get("file_delete_performed"),\n        "destructive_cleanup_performed": summary.get("destructive_cleanup_performed"),\n        "exchange_submit_performed": summary.get("exchange_submit_performed"),\n        "trading_action_performed": summary.get("trading_action_performed"),\n        "training_performed": summary.get("training_performed"),\n        "reload_performed": summary.get("reload_performed"),\n        "runtime_overlay_activated": summary.get("runtime_overlay_activated"),\n    }\n    out_dir = Path(args.reports_dir)\n    if not out_dir.is_absolute():\n        out_dir = ROOT / out_dir\n    out_dir.mkdir(parents=True, exist_ok=True)\n    stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime())\n    suffix = "ready" if ok else "not_ready"\n    report_path = out_dir / f"4B436633I_H1_source_33h_gate_hotfix_{stamp}_{suffix}.json"\n    report_path.write_text(json.dumps(payload, indent=2, sort_keys=True, ensure_ascii=False), encoding="utf-8")\n    payload["report_path"] = str(report_path)\n    print(json.dumps(payload, sort_keys=True, ensure_ascii=False))\n    return 0\n\n\nif __name__ == "__main__":\n    raise SystemExit(main())\n',
    "tools/rollback_4B436633I_H1_source_33h_gate_hotfix.py": 'from __future__ import annotations\n\nimport json\nimport shutil\nfrom pathlib import Path\n\nPATCH_ID = "4B436633I_H1"\nNEW_FILES = [\n    "README_APPLY_4B436633I_H1.txt",\n    "docs/RECOVERY_CLOSURE_REPORT_SOURCE_33H_GATE_HOTFIX_4B436633I_H1.md",\n    "tests/test_recovery_closure_report_h1_4B436633I_H1.py",\n    "tools/check_4B436633I_H1_source_33h_gate_hotfix.py",\n    "tools/run_4B436633I_H1_source_33h_gate_hotfix.py",\n    "tools/rollback_4B436633I_H1_source_33h_gate_hotfix.py",\n]\nMODIFIED_FILES = ["src/tradebot/recovery_closure_report.py"]\n\n\ndef main() -> int:\n    root = Path.cwd()\n    backups = sorted((root / "tools").glob(f"_patch_backup_{PATCH_ID}_*"), reverse=True)\n    backup_root = backups[0] if backups else None\n    restored: list[str] = []\n    removed: list[str] = []\n    if backup_root:\n        for rel in MODIFIED_FILES:\n            source = backup_root / rel\n            target = root / rel\n            if source.exists():\n                target.parent.mkdir(parents=True, exist_ok=True)\n                shutil.copy2(source, target)\n                restored.append(rel)\n    for rel in NEW_FILES:\n        path = root / rel\n        if path.exists():\n            path.unlink()\n            removed.append(rel)\n    print(json.dumps({"patch_id": PATCH_ID, "rollback_performed": True, "backup_root": str(backup_root or ""), "restored_files": restored, "removed_files": removed}, indent=2, sort_keys=True))\n    return 0\n\n\nif __name__ == "__main__":\n    raise SystemExit(main())\n',
}

REPLACEMENT = 'def _mapping_or_empty(value: Any) -> Mapping[str, Any]:\n    return value if isinstance(value, Mapping) else {}\n\n\ndef _pick_nested(payload: Mapping[str, Any], paths: Sequence[Sequence[str]], default: Any = None) -> Any:\n    for path in paths:\n        current: Any = payload\n        matched = True\n        for key in path:\n            if not isinstance(current, Mapping) or key not in current:\n                matched = False\n                break\n            current = current[key]\n        if matched and current is not None:\n            return current\n    return default\n\n\ndef _pick_bool_nested(payload: Mapping[str, Any], paths: Sequence[Sequence[str]], default: bool = False) -> bool:\n    return _as_bool(_pick_nested(payload, paths, default), default=default)\n\n\ndef _pick_str_nested(payload: Mapping[str, Any], paths: Sequence[Sequence[str]]) -> str | None:\n    return _str_or_none(_pick_nested(payload, paths, None))\n\n\ndef _is_sha256(value: str | None) -> bool:\n    if value is None or len(value) != 64:\n        return False\n    return all(char in "0123456789abcdefABCDEF" for char in value)\n\n\ndef build_source_33h_gate(repo_root: str | Path | None = None) -> Source33HGate:\n    root = _repo_root(repo_root)\n    report_path, payload = _latest_33h_report(root)\n    if report_path is None or payload is None:\n        return Source33HGate(\n            complete=False,\n            report_path=None,\n            decision=None,\n            status=None,\n            manifest_sha256=None,\n            immutable_plan_digest=None,\n            final_no_execution_gate_complete=False,\n            human_approval_token_status=None,\n            source_33g_complete=False,\n            error="33H READY report not found",\n        )\n\n    status = _status_from_payload(payload, _status_from_filename(report_path))\n    decision = _decision_from_payload(payload)\n\n    # 33H can appear as a compact stdout summary or as the persisted full run-report.\n    # The full report stores decisive fields under source_33g_gate,\n    # immutable_plan_digest_ledger, human_approval_token_ledger, and final_no_execution_gate.\n    source_33g_complete = _pick_bool_nested(\n        payload,\n        [\n            ("source_33g_complete",),\n            ("source_33g_gate", "complete"),\n        ],\n        default=False,\n    )\n    final_gate_complete = _pick_bool_nested(\n        payload,\n        [\n            ("final_no_execution_gate_complete",),\n            ("final_no_execution_gate", "complete"),\n        ],\n        default=False,\n    )\n    immutable_complete = _pick_bool_nested(\n        payload,\n        [\n            ("immutable_plan_digest_complete",),\n            ("immutable_plan_digest_ledger", "complete"),\n        ],\n        default=False,\n    )\n    manifest_sha256 = _pick_str_nested(\n        payload,\n        [\n            ("manifest_sha256",),\n            ("source_33g_gate", "manifest_sha256"),\n            ("immutable_plan_digest_ledger", "manifest_sha256"),\n        ],\n    )\n    immutable_digest = _pick_str_nested(\n        payload,\n        [\n            ("immutable_plan_digest",),\n            ("immutable_plan_digest_ledger", "plan_digest"),\n            ("immutable_plan_digest_ledger", "immutable_plan_digest"),\n        ],\n    )\n    token_status = _pick_str_nested(\n        payload,\n        [\n            ("human_approval_token_status",),\n            ("human_approval_token_ledger", "token_status"),\n        ],\n    )\n\n    safety_paths: dict[str, list[tuple[str, ...]]] = {\n        "archive_execution_allowed": [("archive_execution_allowed",), ("final_no_execution_gate", "archive_execution_allowed")],\n        "archive_move_performed": [("archive_move_performed",), ("final_no_execution_gate", "archive_move_performed")],\n        "file_delete_performed": [("file_delete_performed",), ("final_no_execution_gate", "file_delete_performed")],\n        "destructive_cleanup_performed": [("destructive_cleanup_performed",), ("final_no_execution_gate", "destructive_cleanup_performed")],\n        "exchange_submit_performed": [("exchange_submit_performed",), ("final_no_execution_gate", "exchange_submit_performed")],\n        "trading_action_performed": [("trading_action_performed",), ("final_no_execution_gate", "trading_action_performed")],\n        "training_performed": [("training_performed",), ("final_no_execution_gate", "training_performed")],\n        "reload_performed": [("reload_performed",), ("final_no_execution_gate", "reload_performed")],\n        "runtime_overlay_activated": [("runtime_overlay_activated",), ("final_no_execution_gate", "runtime_overlay_activated")],\n        "approved_for_exchange_submit": [("approved_for_exchange_submit",), ("final_no_execution_gate", "approved_for_exchange_submit")],\n        "approved_for_live_real": [("approved_for_live_real",), ("final_no_execution_gate", "approved_for_live_real")],\n        "approved_for_paper_transition": [("approved_for_paper_transition",), ("final_no_execution_gate", "approved_for_paper_transition")],\n        "approved_for_runtime_overlay": [("approved_for_runtime_overlay",), ("final_no_execution_gate", "approved_for_runtime_overlay")],\n    }\n    safety_ok = all(not _pick_bool_nested(payload, paths, default=False) for paths in safety_paths.values())\n\n    complete = bool(\n        status == "READY"\n        and decision == "ARCHIVE_EXECUTION_APPROVAL_LEDGER_READY_FINAL_NO_EXECUTION_GATE_LOCKED"\n        and source_33g_complete\n        and final_gate_complete\n        and immutable_complete\n        and _is_sha256(manifest_sha256)\n        and _is_sha256(immutable_digest)\n        and safety_ok\n    )\n\n    return Source33HGate(\n        complete=complete,\n        report_path=_rel(report_path, root),\n        decision=decision,\n        status=status,\n        manifest_sha256=manifest_sha256,\n        immutable_plan_digest=immutable_digest,\n        final_no_execution_gate_complete=final_gate_complete,\n        human_approval_token_status=token_status,\n        source_33g_complete=source_33g_complete,\n        error=None if complete else "33H report found but closure source gate is incomplete",\n    )\n\n\n'


def _backup_root(root: Path) -> Path:
    stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime())
    return root / "tools" / f"_patch_backup_{PATCH_ID}_{stamp}"


def _replace_source_gate(module_path: Path) -> dict[str, object]:
    text = module_path.read_text(encoding="utf-8")
    start = text.find("def build_source_33h_gate(")
    end = text.find("def _phase_expectations()", start)
    if start == -1 or end == -1:
        if "def _pick_nested(" in text and "source_33g_gate" in text:
            return {"patched": False, "path": module_path.as_posix(), "reason": "already_patched"}
        raise RuntimeError("build_source_33h_gate replacement anchors not found")
    new_text = text[:start] + REPLACEMENT + text[end:]
    module_path.write_text(new_text, encoding="utf-8")
    return {"patched": True, "path": module_path.as_posix(), "reason": "source_33h_gate_replaced"}


def main() -> int:
    root = Path.cwd()
    backup_root = _backup_root(root)
    backup_root.mkdir(parents=True, exist_ok=True)
    modified_files = ["src/tradebot/recovery_closure_report.py"]
    backed_up_files: list[str] = []
    for rel in modified_files:
        source = root / rel
        if source.exists():
            target = backup_root / rel
            target.parent.mkdir(parents=True, exist_ok=True)
            shutil.copy2(source, target)
            backed_up_files.append(rel)

    patch_result = _replace_source_gate(root / "src" / "tradebot" / "recovery_closure_report.py")

    written_files: list[str] = []
    for rel, content in NEW_FILES.items():
        path = root / rel
        path.parent.mkdir(parents=True, exist_ok=True)
        path.write_text(content, encoding="utf-8", newline="\n")
        written_files.append(rel)

    compile_errors: dict[str, str] = {}
    for rel in [*modified_files, *[p for p in NEW_FILES if p.endswith(".py")]]:
        try:
            py_compile.compile(str(root / rel), doraise=True)
        except Exception as exc:
            compile_errors[rel] = str(exc)

    payload = {
        "applied": not compile_errors,
        "patch_id": PATCH_ID,
        "patch_version": PATCH_VERSION,
        "patch_name": PATCH_NAME,
        "source_gate_patch_result": patch_result,
        "backed_up_files": backed_up_files,
        "backup_root": str(backup_root.relative_to(root)),
        "modified_files": modified_files,
        "written_files": written_files,
        "py_compile_ok": not compile_errors,
        "compile_errors": compile_errors,
        "approved_for_live_real": False,
        "approved_for_paper_transition": False,
        "approved_for_exchange_submit": False,
        "approved_for_runtime_overlay": False,
        "next_phase_unlock_performed": False,
        "archive_execution_allowed": False,
        "archive_move_performed": False,
        "file_delete_performed": False,
        "destructive_cleanup_performed": False,
        "exchange_submit_performed": False,
        "trading_action_performed": False,
        "training_performed": False,
        "reload_performed": False,
        "runtime_overlay_activated": False,
    }
    print(json.dumps(payload, indent=2, sort_keys=True, ensure_ascii=False))
    return 0 if not compile_errors else 1


if __name__ == "__main__":
    raise SystemExit(main())
