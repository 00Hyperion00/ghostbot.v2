document.addEventListener('DOMContentLoaded',()=>{const mark=()=>fetch('/v3/notifications/read',{method:'POST',headers:{'X-CSRF-Token':window.V3_CSRF||''}}).catch(()=>{});document.querySelectorAll('.badge').forEach(b=>{if(Number(b.textContent)>0)b.addEventListener('click',mark)});document.querySelectorAll('form').forEach(f=>f.addEventListener('submit',()=>{const btn=f.querySelector('button[type=submit],button:not([type])');if(btn){btn.dataset.old=btn.textContent;btn.textContent='İşleniyor…';btn.disabled=true}}));});
